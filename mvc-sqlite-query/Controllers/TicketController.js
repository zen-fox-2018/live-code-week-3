const ModelTicket = require('../Models/Tickets');
const view = require('../view');
const ShowController = require('./ShowController');
class TicketController {
    static buyTickets(command) {
        console.log(command)

    }
}
module.exports = TicketController